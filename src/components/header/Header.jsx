import logo from "../../assets/logo.svg";

const Header = () => {
  return (
    <header>
        <div>
            <img src={logo} alt="logo" />
        </div>
        <div></div>
    </header>
  )
}

export default Header